type Product {
  id: ID!
  name: String!
  price: Float!
  inStock: Boolean!
}

type Query {
  getProduct(id: ID!): Product
  listProducts: [Product]
}

type Mutation {
  addProduct(name: String!, price: Float!): Product
}